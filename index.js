const primeiroNumnero = 5;
const segundoNumero = 7;
const resultado = primeiroNumnero + segundoNumero;
const duplicateResult = resultado * 3
const text = "Isso aqui e um texto"
//manipular com let

let duplicar = 0;
duplicar = resultado * 5
console.log(duplicateResult, duplicar);
console.log(typeof primeiroNumnero, typeof text);

//Todos os operadores matematicos 
/**
 * 
 * +,-,/,*
 * Const não pode ser modificado
 * String = Texto | Number = Numero
 * JavaScript e possui uma tipagem dinamica, sendo assim e de 
 * suma importancia declarar o tipo certo
 * Mas em caso de incerteza e não souber qual é o tipo, usar metodo
 * Typeof(nomeVariavel)
 */

/**
 * Operadores de Atribuição
 * =, +=, -=, *=, /=, %=
 * Dados primitivos
 * Undefined = Não definido
 * Null = Nulo
 * Boolean = Verdadeiro ou Falso
 * Number = Numero
 * String = Texto
 * Object = Objeto
 * Array = Vetor
 * Function = Função
 * Date = Data
 */
