// Os operadores ternarios é ? e :
// O operador ternario é um operador que tem a seguinte sintaxe:
// condição ? valor1 : valor2
const valor = 100;
const newUser = valor >= 300 ? "Saldo Positivo" : "Saldo Negativo";
console.log(newUser);